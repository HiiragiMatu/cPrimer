#include <stdio.h>

int main(void) {
  int toes = 10;
  int double_toes = 2 * toes;
  int square_toes = toes * toes;

  printf("Toes is %d, and its double and square are %d and %d respectively.", toes, double_toes, square_toes);
  return 0;
}